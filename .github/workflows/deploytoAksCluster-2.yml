on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n tortoolkit2001 -g tortoolkit2001819b-rg --location "East US" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n tortoolkit2001 --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n tortoolkit2001 --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n tortoolkit2001 --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: tortoolkit2001.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t tortoolkit2001.azurecr.io/dsileecher:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push tortoolkit2001.azurecr.io/dsileecher:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_dsileecher_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep dsileechera1b4243 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace dsileechera1b4243`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: dsileechera1b4243
        container-registry-url: tortoolkit2001.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: dsileecherdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: dsileechera1b4243
        manifests: |
          manifests/deployment-2.yml
          manifests/service-2.yml
        images: |
          tortoolkit2001.azurecr.io/dsileecher:${{ github.sha }}
        imagepullsecrets: |
          dsileecherdockerauth   
