on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n dsileecher234567 -g dsileecher2345678326-rg --location "East US" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n dsileecher234567 --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n dsileecher234567 --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n dsileecher234567 --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: dsileecher234567.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t dsileecher234567.azurecr.io/dsileecher:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push dsileecher234567.azurecr.io/dsileecher:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_dsileecher_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep dsileecherbf26q43222 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace dsileecherbf26q43222`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: dsileecherbf26q43222
        container-registry-url: dsileecher234567.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: dsileecherdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: dsileecherbf26q43222
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          dsileecher234567.azurecr.io/dsileecher:${{ github.sha }}
        imagepullsecrets: |
          dsileecherdockerauth   
