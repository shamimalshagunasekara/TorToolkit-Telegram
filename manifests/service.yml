apiVersion: v1
kind: Service
metadata:
    name: "dsileecher-dce6"
    labels:
        app: "dsileecher-dce6"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "dsileecher-dce6"